import React from "react";

// This component is now just a placeholder as we've moved functionality to TitleEqualizer
const Equalizer: React.FC = () => {
  return null;
};

export default React.memo(Equalizer);
